@page "/access-denied"
@layout EmptyLayout
@* Sử dụng EmptyLayout để trang này không hiển thị sidebar hoặc header không cần thiết *@

@inject NavigationManager Navigation

<div class="container text-center mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0">
                <div class="card-body p-5">
                    <i class="fas fa-user-lock fa-5x text-danger mb-4"></i>
                    <h1 class="display-5 fw-bold text-danger">Truy Cập Bị Từ Chối</h1>
                    <p class="lead text-muted mb-4">
                        Rất tiếc, bạn không có quyền truy cập vào trang này.
                        <br>
                        Tài khoản của bạn có thể không được cấp phép cho chức năng này.
                    </p>
                    <hr class="my-4">
                    <p class="mb-4">
                        Nếu bạn cho rằng đây là một sự nhầm lẫn, vui lòng liên hệ với quản trị viên hệ thống để được hỗ trợ.
                    </p>
                    
                    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                        <button @onclick="GoBack" class="btn btn-secondary btn-lg px-4 gap-3">
                            <i class="fas fa-arrow-left me-2"></i> Quay Lại
                        </button>
                        <button @onclick="GoToHome" class="btn btn-primary btn-lg px-4">
                            <i class="fas fa-home me-2"></i> Về Trang Chủ
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    [Inject]
    private IJSRuntime JSRuntime { get; set; }

    // Điều hướng người dùng quay lại trang trước đó
    private async Task GoBack()
    {
        // Sử dụng JavaScript interop để thực hiện chức năng "back" của trình duyệt
        await JSRuntime.InvokeVoidAsync("history.back");
    }

    // Điều hướng người dùng về trang chủ
    private void GoToHome()
    {
        Navigation.NavigateTo("/");
    }
}